apiVersion: v1
kind: Service
metadata:
  name: {{ include "marine-k8-integration.fullname" . }}
  labels:
    {{- include "marine-k8-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marine-k8-integration.selectorLabels" . | nindent 4 }}
