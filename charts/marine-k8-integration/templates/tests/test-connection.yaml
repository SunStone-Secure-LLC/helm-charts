apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "marine-k8-integration.fullname" . }}-test-connection"
  labels:
    {{- include "marine-k8-integration.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "marine-k8-integration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
